#include<bits/stdc++.h>
#define ll long long
using namespace std;
int floorMod(int a, int n)   // this is the python mod
{
   
    int q = (int)floor((double)a / n);
 
    return a - n * q;
}
int main()
{
    int x=0&1;
    cout<<x;
}